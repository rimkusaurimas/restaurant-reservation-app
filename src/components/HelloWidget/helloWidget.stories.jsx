import React from "react";
import { HelloWidget } from "./HelloWidget";

export default {
  title: "Components / HelloWidget",
  component: HelloWidget,
};

export const HelloWidgetComponent = () => <HelloWidget />;
